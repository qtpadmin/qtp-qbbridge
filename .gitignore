# .NET build output
bin/
obj/
*.user
*.suo

# Self-contained published binaries (regenerated by `dotnet publish`)
publish/

# Release dir is entirely build-generated (duplicates of deploy/ + src/) — regenerated by build
release/

# Editor / IDE
.vs/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Local logs (never produced in source tree, but defensive)
*.log
